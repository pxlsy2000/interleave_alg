# Hash Interleave 分析工具产品规格

## 这是什么

Hash Interleave 分析工具用于回答两个问题：

1. 一套地址映射是否正确；
2. 这套映射在典型访问模式下是否足够均衡。

工具面向设计、验证和性能分析人员。使用者不需要阅读或理解工具本身的代码，只需要描述地址映射和访问场景，就能得到可解释、可复查的结论。

本规格只定义工具应该呈现给使用者的能力和行为，不规定编程语言、依赖库、内部数据结构或工程组织方式。

## 使用者希望完成的事情

使用者可以：

- 创建一份地址映射说明；
- 检查这份映射是否满足基本数学约束；
- 查询任意地址会被映射到哪个 Target，以及它在该 Target 内的 Local Address；
- 描述顺序访问、跨步访问和多路并发访问等场景；
- 比较不同映射在长期均衡、短时拥塞和连续冲突方面的表现；
- 得到适合人工评审的报告；
- 在需要进一步处理结果时导出结构化报告。

## 核心概念

### 地址、粒度和 Target

一个 byte address 由两部分组成：

- 粒度内部的 byte offset；
- 粒度以上、真正参与映射的 line address。

映射只改变 line address。粒度内部的 byte offset 必须原样保留。

每个输入地址最终得到：

- 一个 Target；
- 一个该 Target 内的 Local Address，简称 LA。

### Mapping

Mapping 描述 line address 的各个 bit 如何通过 XOR 组合产生 Target 和 LA。

使用者可以把每个输出 bit 表达为若干输入 bit 的 XOR。例如，“由输入 bit 0、4、8 共同决定”表示这三个 bit 的异或结果就是该输出 bit。

Target 的映射规则记为 `M`，LA 的映射规则记为 `L`。二者共同定义一个地址最终落到哪里。

### 正确性的三个层次

工具必须区分以下三个结论，不能把它们混为一谈：

1. **Target 可达**

   每个 Target 都有地址能够访问到。

2. **Mapping 双射**

   每个输入 line address 都映射到唯一的 `(Target, LA)`，并且有效输出空间中没有重复，也没有空洞。

3. **每个 Target 内的 LA 自然有序**

   固定一个 Target 后，输入地址对应的 LA 能按自然顺序完整排列，而不是虽然不重复、但发生了 bit 置换或重排。

一套 Mapping 可能满足双射，但不满足 LA 自然有序。此时工具应明确说明“映射有效，但 LA 顺序不自然”，而不是把它误报为映射无效。

## 主要使用流程

### 1. 准备 Mapping

使用者描述：

- 地址宽度；
- 访问粒度；
- Target 数量；
- Target 各输出 bit 的 XOR 规则；
- LA 的生成方式。

工具应提供一份带说明的示例，帮助使用者理解每项信息的含义。示例应当可以直接用于后续验证和分析。

对于 LA，工具至少应支持：

- 保留未用于 Target 编号的高位地址，使 LA 保持自然顺序；
- 由使用者明确描述每个 LA bit 的 XOR 规则。

### 2. 验证 Mapping

验证结果应逐项回答：

- 地址宽度、粒度和 Target 数量是否彼此兼容；
- Mapping 中引用的 bit 是否存在且没有歧义；
- 每个 Target 是否可达；
- 整体 Mapping 是否为双射；
- 每个 Target 内的 LA 是否自然有序。

报告必须解释失败发生在哪一项，以及它对实际映射意味着什么。只给出“合法”或“不合法”不足以满足要求。

如果工具暂不支持某类地址空间或 Target 组织方式，应明确说“暂不支持”，不能把它描述成 Mapping 本身错误。

### 3. 查询具体地址

使用者可以输入一个或多个地址，查看每个地址的：

- 原始 byte address；
- line address；
- Target；
- LA line；
- 包含原始 byte offset 的最终 LA。

该能力用于人工抽查 Mapping，也用于解释性能报告中出现的异常。

### 4. 描述访问场景

一个场景表示一段有确定先后顺序的地址访问。

工具至少应覆盖：

- **单路跨步访问**：从一个起始地址出发，以固定步长连续访问；
- **起始相位与步长扫描**：比较多种起始地址和步长组合；
- **多路访问**：多个访问流按明确的顺序交替发出请求。

每个场景可以指定访问次数和需要观察的时间窗口。多个场景可以放在一起，使用者既可以运行全部启用的场景，也可以只选择其中一部分。

相同的 Mapping 和场景必须产生相同的访问顺序和分析结果，以便复查和比较。

如果场景生成了超出有效地址范围的地址，工具应指出具体场景和原因，不应静默截断或回绕。

## Mapping 验证规则

### 基本约束

当前范围只考虑：

- Target 等容量；
- Target 数量为 2 的幂；
- 完整、无空洞的地址空间；
- 粒度为 2 的幂；
- byte offset 不参与 Target 和 LA line 的计算。

不满足上述范围的配置应被标记为“当前版本不支持”。

### Target 可达

Target 的映射规则必须能够产生所有 Target 编号。

如果某些 Target 永远无法被选中，报告应列出该问题，并说明这会导致地址空间分布不完整。

### Mapping 双射

Target 与 LA 的组合必须保留输入 line address 的全部信息。

如果两个不同输入地址得到相同的 `(Target, LA)`，Mapping 不是双射。报告应说明存在地址碰撞或输出空间空洞。

### LA 自然有序

固定任意一个 Target 后，LA 应覆盖该 Target 的完整本地地址空间，并保持自然的二进制顺序。

如果 LA 只是发生 bit 置换，Mapping 仍可被判定为双射，但报告必须给出单独警告，说明它不满足自然顺序要求。

## 性能分析

工具根据场景生成有序地址序列，再将其转换为 Target 访问序列。

每个场景至少报告以下指标。

### 各 Target 访问量

报告每个 Target 的访问次数和占总访问量的比例，用于观察长期分布。

### 最大负载率

最大负载率用于表示最繁忙 Target 相对于理想平均负载的倍数：

```text
最大负载率 = 最繁忙 Target 的访问次数 / 理想的每 Target 平均访问次数
```

结果为 `1` 表示长期分布完全均衡。数值越大，表示最繁忙 Target 的负载偏离理想值越多。

### 短时拥塞

长期均衡不代表短时间内没有拥塞。工具应在使用者指定的不同窗口大小下，寻找负载最不均衡的连续访问片段。

对于每个窗口，报告至少包括：

- 最差负载率；
- 受影响的 Target；
- 最差窗口在访问序列中的位置；
- 该 Target 在窗口内的访问次数。

### 连续访问

报告连续访问同一个 Target 的最长长度。

这个指标用于发现“总量均衡，但请求成团到达”的情况。

### 多路访问

多路场景必须采用清晰、固定的请求合并顺序。报告应让使用者能够区分：

- 多路访问在相同相位下叠加；
- 多路访问错开相位；
- 两者对短时拥塞和连续访问造成的差异。

## 报告要求

### 面向人工评审的报告

默认报告应优先帮助人理解结论，而不是暴露内部计算过程。

Mapping 验证报告应包含：

- 输入配置的简要摘要；
- 每项验证的通过、警告或失败状态；
- 最终结论；
- 失败或警告的实际含义。

性能报告应包含：

- 场景名称和访问总数；
- 各 Target 的访问量与占比；
- 最大负载率；
- 各窗口的最差短时拥塞；
- 最长连续同 Target 访问；
- 需要关注的异常说明。

报告中的专业术语应在首次出现时解释。详细的矩阵展开可以作为辅助信息按需查看，不应淹没默认结论。

### 结构化报告

工具可以导出结构化结果，供自动化处理或不同 Mapping 之间的比较使用。

结构化报告与人工报告必须表达相同的结论，不能出现一方认为通过、另一方认为失败的情况。

## 错误与提示

错误信息必须告诉使用者：

- 哪份 Mapping 或哪个场景有问题；
- 哪一项输入导致问题；
- 为什么它无效或暂不支持；
- 使用者应检查什么。

以下情况应明确区分：

- Mapping 输入无法理解；
- Mapping 违反正确性约束；
- 场景描述无效；
- 场景生成的地址超出范围；
- 分析未能完成；
- 输入本身合理，但属于当前版本暂不支持的能力。

工具不能静默修正会改变语义的输入。

## 验收标准

满足以下条件时，可以认为本规格已实现。

### Mapping

- 示例 Mapping 可以直接完成验证、地址查询和场景分析；
- 合法 Mapping 能正确区分 Target 可达、整体双射和 LA 自然有序；
- Target 不可达时，结果明确失败并解释影响；
- 存在地址碰撞时，结果明确失败并解释影响；
- Mapping 双射但 LA bit 被重排时，结果通过并给出独立警告；
- 任意地址查询都保留原始 byte offset；
- 对一个足够小、可穷举的地址空间，所有 `(Target, LA)` 既不重复也不遗漏。

### 场景与指标

- 单路跨步场景生成的地址数量、顺序和步长符合描述；
- 起始相位与步长扫描覆盖所有要求的组合；
- 多路场景按规定顺序合并请求，且结果可重复；
- 对长期访问量相同、但访问顺序不同的两个序列，最大负载率可以相同，而短时拥塞指标能够体现差异；
- 相同相位与错开相位的多路访问能够产生可解释的不同结果；
- 地址越界时分析停止，并指出具体场景和越界原因。

### 报告

- 不阅读源代码也能从报告中判断 Mapping 是否有效；
- 警告和失败均说明对实际地址映射或性能的影响；
- 人工报告与结构化报告的结论一致；
- 默认报告聚焦结论，详细矩阵只在使用者需要时展示。

## 当前不包含的能力

以下能力不属于当前范围：

- Target 数量不是 2 的幂；
- Target 容量不相等；
- 地址空间中存在空洞；
- 导入真实硬件 trace；
- 除固定交替顺序之外的复杂多路调度策略。

这些能力未来可以扩展，但不应影响当前范围内结果的准确性和可解释性。
